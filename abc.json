{
  "assets": {
    "type": "command",
    "command": {
      "cmd": [
        "ayarn",
        "npm run build",
        "mkdir $BUILD_DEST/dist",
        "mv ./dist $BUILD_DEST/dist",
        "mv ./es $BUILD_DEST",
        "mv ./package.json $BUILD_DEST"
      ]
    }
  }
}